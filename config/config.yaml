vector_db:
  index_name: "trade-agent-1"
  dimension : 3072
  metric : "cosine"

retriever:
  top_k: 3
  score_threshold: 0.5

embedding_model:
  provider: "google"
  model_name: "models/gemini-embedding-2-preview"

llm:
  google:
    provider: "google"
    model_name: "gemini-1.5-pro"
  groq:
    provider: "groq"
    model_name: "deepseek-r1-distill-llama-70b"

tools:
  tavily:
    max_results: 5
